{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "routes": [
    {
      "source": "/api/(.*)",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
      "headers": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ]
}
